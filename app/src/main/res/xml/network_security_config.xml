<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext HTTP is permitted for local network server connections (LAN, WiFi, Tailscale, emulator). -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
        Cleartext is only permitted for the local/private endpoints this app
        is designed to reach directly:
          - localhost / 127.0.0.1: the device talking to itself (e.g. emulator, adb reverse).
          - 10.0.2.2: the Android emulator's alias for the host machine's loopback.
          - ts.net (with subdomains): Tailscale MagicDNS names. This traffic is already
            encrypted at the network layer by WireGuard, so plaintext HTTP over it is
            not a real exposure.
        Connecting to a server by raw LAN IP (e.g. 192.168.x.x) is NOT covered here and
        requires either serving over HTTPS or explicitly adding that IP to this list.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="true">ts.net</domain>
    </domain-config>
</network-security-config>
