*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Local Pi runtime state
.atl/
# Generated bearer token for the local bridge servers
.idupi-token
# Runtime projects database
idupi-server/projects.json
# Local Pi CLI agent runtime state
.pi/
# Build/debug artifacts that shouldn't live in git
*.apk
# Exception: the ready-to-install APK is committed at the repo root for end users.
!IDUPI-ap.apk
!/IDUPI-ap.apk
*.stackdump
# Release signing - never commit keystores or passwords
*.keystore
*.jks
*.p12
keystore.properties
signing.properties
# Pi subagent run artifacts (transcripts, cost meta) -- runtime state, not source
.pi-subagents/
# Pi subagent role outputs: pi-subagents declares these in each role's
# frontmatter (`output:`), so they reappear in the repo root on every run.
/context.md
/research.md
